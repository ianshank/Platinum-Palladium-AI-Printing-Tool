# DEV-SQE → PRE-PR Handoff

**Date**: 2026-09-18 13:11:57 UTC
**Session ID**: e274d4ad-70e6-5d62-ba38-6b277fa971b0
**Branch**: claude/ptpd-validation-sdlc-plan-j2a2gc
**Commit**: d60bc24
**Pull request**: https://github.com/ianshank/Platinum-Palladium-AI-Printing-Tool/pull/37
**CI run**: https://github.com/ianshank/Platinum-Palladium-AI-Printing-Tool/actions/runs/35348644644

Numbers (test counts, coverage) are not written into this document; read them
from the CI run above (ADR-0007).

## Implementation summary

Phase 0 of `docs/plans/2026-09-validation-sdlc-plan.md` is implemented. The
goal was to make "green" mean something before any feature work, so every
item below is either a gate, a defect the gate exposed, or a fix for a claim
the repository made about itself that was not true.

## Tasks completed

- **TASK-002** One consolidated, SHA-pinned CI workflow with a single required
  check (`all-green`), tag-only release, and a deploy job that fails loudly on
  a missing token. `ci-cd.yml` and `tests.yml` are deleted.
- **TASK-003** uv migration: committed `uv.lock`, dependencies the package
  actually imports declared in `pyproject.toml`, PEP 735 dependency groups and
  extras. The package imports from its own metadata again.
- **TASK-004** Red backend tests fixed (the undefined `paper_stats` name).
- **TASK-005** Red frontend tests fixed; store typed; lint baseline to zero.
- **TASK-006** ruff and mypy baselines at zero (mypy on an allowlist of
  packages that reach zero errors, widened by later phases).
- **TASK-007** Strict pytest: `--strict-markers --strict-config`, registered
  markers, timeout, and `filterwarnings = ["error", ...]`.
- **TASK-008** Repo hygiene: vendored tooling untracked, pre-commit, CODEOWNERS,
  Dependabot.
- **TASK-009** Immediate security fixes (see below).
- **TASK-010** Documentation reduced to the canonical root allowlist, enforced
  by `scripts/check-doc-sprawl.sh` in CI and pre-commit; 15 ADRs written.
- **TASK-012** README metrics generated by `scripts/test_metrics.py` from CI
  artifacts instead of typed into markdown.

## Defects found and fixed

| ID | Where | Defect |
|---|---|---|
| F1 | `curves/modifier.py` | SPLINE smoothing dropped to a 3-knot fit, collapsing short curves to a near-straight line |
| F2 | `curves/modifier.py` | `preserve_endpoints` pinned only the ends, so a monotone input could come back non-monotone |
| F3 | `curves/export.py` | a `.quad` written by `QTRExporter` could not be read back by `load_curve` |
| TST-01 | `session/logger.py` | undefined `paper_stats` name |
| ARC-06 | `api/server.py` | MCTS search endpoint returned a shaped placeholder instead of running the engine |
| ADR-0006 | `ml/` | simulated densities were stored in fields named `measured_*` |
| SEC-01/02/03/07 | `api/`, `imaging/`, `curves/`, `core/` | path traversal, unbounded upload, decompression bomb, unsafe artifact load, CORS credentials with a wildcard origin |

Warnings-as-errors additionally exposed, and this branch fixes at the source:
pyplot figure retention in `curves/visualization.py`, a deprecated Pillow
`mode=` argument, an unclosed log file handle, `np.corrcoef` on constant
input, `np.mean([])` on optional readings, and cross-module environment
leakage that made settings-default assertions order-dependent.

## Verification

Run locally before the push, and again by the CI run linked above:

```bash
uv run pytest -m "not selenium and not visual and not performance and not browser" --cov --cov-branch
uv run ruff check src tests app.py scripts && uv run ruff format --check src tests app.py scripts
uv run mypy
bash scripts/check-doc-sprawl.sh
cd frontend && pnpm check:all && pnpm test:run && pnpm build
```

All four tiered coverage floors (mcts, chemistry, curves, whole) are met.

## Verification checklist for PRE-PR

- [x] Tests pass and no test is skipped or quarantined to get there
- [x] Lint, format, type and documentation gates pass
- [x] Coverage floors met
- [x] CHANGELOG entry present
- [x] ADRs written for every decision this branch encodes
- [ ] Security report findings confirmed fixed before their details are
      committed (AGENTS.md rule 10) — the report stays out-of-band
- [ ] Owner actions below performed

## Blocked on the owner (plan §9)

1. Make `main` the default branch and fast-forward it to this trunk (ADR-0001).
2. Add branch and tag rulesets requiring `all-green`.
3. Close the superseded pull requests (#13, #14, #16, #17, #34, #36).
4. Create the deploy environment and its `HF_TOKEN` secret, or leave the
   deploy job disabled.

## Known gaps carried forward (not Phase 0)

- ARC-02 deep-learning journeys are strict `xfail` with plan items, not fixed.
- SCI-01/02 keep two metamorphic relations as strict `xfail`.
- The React app still cannot export a digital negative, so the Gradio UI
  cannot be retired yet (ADR-0004).
- The frontend charts chunk is far above the bundle-size target; code-splitting
  is a Phase 1 item.

---
**Handoff Document**: immutable after creation
**Next Phase**: PRE-PR validation
